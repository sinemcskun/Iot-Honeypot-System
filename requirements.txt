# ============================================================
# IoT Honeypot Pipeline — Requirements
# ============================================================

# --- Common ---
pandas
numpy
pyyaml

# --- Edge (MQTT transport) ---
paho-mqtt

# --- Central (Database) ---
# (uses stdlib sqlite3)

# --- Analysis (ML pipeline) ---
scikit-learn
xgboost
pyarrow          # parquet read/write
joblib           # model serialization

# --- Dashboard ---
streamlit
plotly

# --- LLM Deception Pipeline (llm/) ---
torch
transformers
peft             # LoRA / QLoRA adapters
trl              # SFTTrainer
datasets         # Hugging Face dataset loader
bitsandbytes     # 4-bit quantization
nltk             # BLEU score
rouge-score      # ROUGE metrics
accelerate       # HF model loading
